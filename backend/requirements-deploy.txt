# Clean, PyPI-installable dependencies for external hosting (Render/Railway/Fly).
# Use this instead of the full requirements.txt (which contains Emergent-only
# packages like emergentintegrations and a custom litellm wheel that will fail
# pip install off-platform and are NOT imported by server.py).
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic==2.13.4
email-validator==2.3.0
python-dotenv==1.2.3
PyJWT==2.13.0
bcrypt==4.1.3
httpx==0.28.1
reportlab==5.0.1
python-multipart==0.0.32
